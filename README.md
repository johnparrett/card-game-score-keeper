# Score Keeper (web)

Minimal web implementation of a Rummy/Chinchón-style scorekeeper.

Quick start:

```bash
cd score-keeper
npm install
npm run dev
```

This project is scaffolded with Vite + React + TypeScript. It stores data in `localStorage` and supports export/import of JSON backups.
